version: 2

models:
  - name: dim_clientes
    description: Clientes da Northwind.
    columns:
      - name: id_cliente
        description: Chave primária da tabela clientes.
        tests:
          - unique
          - not_null
